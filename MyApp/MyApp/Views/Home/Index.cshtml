@{
    ViewData["Title"] = "Home Page";
}

<div class="space-y-6 text-center">
    <h1 class="text-4xl font-bold tracking-tight text-gray-900 sm:text-5xl">Welcome to myInventory</h1>
    <p class="text-lg text-gray-600">
        Learn about
        <a href="https://learn.microsoft.com/aspnet/core" class="font-semibold text-indigo-600 hover:text-indigo-500">building Web apps with ASP.NET Core</a>.
    </p>
</div>

<div class="mt-10 grid gap-6 sm:grid-cols-2">
    <div class="bg-gray-800/50 outline -outline-offset-1 outline-white/10 sm:rounded-lg">
        <div class="px-4 py-5 sm:p-6">
            <h3 class="text-base font-semibold text-white">Añadir nuevo usuario</h3>
            <div class="mt-2 max-w-xl text-sm text-gray-400">
                <p>Registra a un nuevo miembro del equipo y asigna sus permisos para que pueda gestionar el inventario sin retrasos.</p>
            </div>
            <div class="mt-5">
                <a asp-area="" asp-controller="Home" asp-action="CreateUser" data-create-user-button="true" class="inline-flex items-center rounded-md bg-indigo-500 px-3 py-2 text-sm font-semibold text-white hover:bg-indigo-400 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-500">Crear usuario</a>
            </div>
        </div>
    </div>
    <div class="bg-gray-800/50 outline -outline-offset-1 outline-white/10 sm:rounded-lg">
        <div class="px-4 py-5 sm:p-6">
            <h3 class="text-base font-semibold text-white">Añadir nuevo artículo</h3>
            <div class="mt-2 max-w-xl text-sm text-gray-400">
                <p>Introduce un nuevo item en el inventario principal con su descripción y stock para mantener el control actualizado.</p>
            </div>
            <div class="mt-5">
                <button type="button" class="inline-flex items-center rounded-md bg-indigo-500 px-3 py-2 text-sm font-semibold text-white hover:bg-indigo-400 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-500">Registrar artículo</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        (function () {
            const createUserButtons = document.querySelectorAll('[data-create-user-button="true"]');
            const createUserUrl = '@Url.Action("CreateUser", "Home")';

            createUserButtons.forEach(function (button) {
                button.addEventListener('click', function (event) {
                    event.preventDefault();
                    window.location.href = createUserUrl;
                });
            });
        })();
    </script>
}
